import { useState } from 'react'  const fotos = [   { src: 'https://via.placeholder.com/600x400/8B4513/FFFFFF?text=Evento+1', alt: 'Buffet de casamento' },<br/>   { src: 'https://via.placeholder.com/600x400/D2B48C/000000?text=Evento+2', alt: 'Decoração de festa' },<br/>   { src: 'https://via.placeholder.com/600x400/BC8F8F/FFFFFF?text=Evento+3', alt: 'Mesa de doces' },<br/>   { src: 'https://via.placeholder.com/600x400/8FBC8F/FFFFFF?text=Evento+4', alt: 'Buffet corporativo' },<br/>   { src: 'https://via.placeholder.com/600x400/F5DEB3/000000?text=Evento+5', alt: 'Coquetel' },<br/>   { src: 'https://via.placeholder.com/600x400/DEB887/000000?text=Evento+6', alt: 'Festa infantil' } ]  export default function Galeria() {   const [lightboxOpen, setLightboxOpen] = useState(false)   const [selected, setSelected] = useState(null)    const openLightbox = (index) => {     setSelected(index)     setLightboxOpen(true)   }    const closeLightbox = () => {     setLightboxOpen(false)     setSelected(null)   }    const next = () => {     if (selected !== null) setSelected((selected + 1) % fotos.length)   }    const prev = () => {     if (selected !== null) setSelected((selected - 1 + fotos.length) % fotos.length)   }    return (     <section id="galeria" className="galeria">       <div className="container">         <h2 className="section-title">Galeria de Eventos</h2>         <p className="section-subtitle">Veja alguns dos eventos que já realizamos</p>         <div className="galeria__grid">           {fotos.map((foto, index) => (             <div key={index} className="galeria__item" onClick={() => openLightbox(index)}>               <img src={foto.src} alt={foto.alt} />               <div className="galeria__overlay">                 <span>Ampliar</span>               </div>             </div>           ))}         </div>       </div>        {lightboxOpen && selected !== null && (         <div className="lightbox" onClick={closeLightbox}>           <span className="lightbox__close">&times;</span>           <button className="lightbox__nav lightbox__prev" onClick={(e) => { e.stopPropagation(); prev() }}>&#8249;</button>           <img className="lightbox__img" src={fotos[selected].src} alt={fotos[selected].alt} onClick={(e) => e.stopPropagation()} />           <button className="lightbox__nav lightbox__next" onClick={(e) => { e.stopPropagation(); next() }}>&#8250;</button>         </div>       )}     </section>   ) }
