export default function Hero() {   return (     <section id="hero" className="hero">       <div className="hero__overlay"></div>       <div className="hero__content">         <h1 className="hero__title">Transformamos seu evento em uma experiência inesquecível</h1>         <p className="hero__subtitle">Buffet, decoração e organização para festas, casamentos e eventos corporativos</p>         <a href="#contato" className="hero__cta" onClick={(e) => { e.preventDefault(); document.getElementById('contato')?.scrollIntoView({ behavior: 'smooth' }) }}>           Solicite um orçamento         </a>       </div>     </section>   ) }
